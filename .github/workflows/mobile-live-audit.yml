name: Mobile live audit

on:
  push:
    branches: [main]
    paths:
      - 'client/**'
      - '.github/workflows/mobile-live-audit.yml'
  pull_request:
    branches: [main]
    paths:
      - 'client/**'
      - '.github/workflows/mobile-live-audit.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mobile-live-audit-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    name: Chromium phone operability
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: client/package-lock.json

      - name: Install browser audit dependency
        working-directory: client
        run: npm install --no-save --package-lock=false --no-audit --no-fund playwright@1.54.1

      - name: Install Chromium
        working-directory: client
        run: npx playwright install --with-deps chromium

      - name: Wait briefly for Render and audit four phone sizes
        working-directory: client
        env:
          MOBILE_AUDIT_URL: https://henan-50k.onrender.com
          EXPECTED_UI_RELEASE: ink-06-mobile-r4
          RELEASE_TIMEOUT_MS: 180000
        run: node mobile-live-audit.mjs

      - name: Upload phone screenshots and report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-live-audit-${{ github.run_attempt }}
          path: client/mobile-audit-artifacts
          if-no-files-found: warn
          retention-days: 14
